let mpMode='create';function openMultiplayer(mode='create'){mpMode=mode;show('multiplayer');const title=document.getElementById('mpTitle'),sub=document.getElementById('mpSubtitle'),name=document.getElementById('roomName'),code=document.getElementById('roomCodeInput'),btn=document.getElementById('mpMainButton'),info=document.getElementById('roomInfo');document.getElementById('mpForm').hidden=false;info.hidden=true;name.hidden=mode==='join';code.hidden=mode==='create';if(mode==='create'){title.innerHTML='Create a <span>Room</span>';sub.textContent='Create a room code and share it with classmates.';btn.textContent='Create Room';btn.onclick=createStudyRoom}else{title.innerHTML='Join a <span>Room</span>';sub.textContent='Enter the room code shared by your classmate.';btn.textContent='Join Room';btn.onclick=joinStudyRoom}}function randomCode(){return Math.random().toString(36).slice(2,8).toUpperCase()}function profile(){try{return JSON.parse(localStorage.getItem('sqStudentProfile')||'{}')}catch(e){return{}}}function createStudyRoom(){const code=randomCode(),p=profile(),room={code,name:document.getElementById('roomName').value.trim()||'StudyQuest Room',host:p.name||'Student',createdAt:Date.now(),players:[{id:p.id||'local',name:p.name||'Student'}]};localStorage.setItem('sqRoom_'+code,JSON.stringify(room));showRoom(room)}function joinStudyRoom(){const code=document.getElementById('roomCodeInput').value.trim().toUpperCase();if(!code)return alert('Please enter a room code.');const raw=localStorage.getItem('sqRoom_'+code);if(!raw)return alert('Room not found. For live rooms across different devices, connect Supabase Realtime.');const room=JSON.parse(raw),p=profile();if(!room.players.some(x=>x.id===(p.id||'local'))){room.players.push({id:p.id||'local',name:p.name||'Student'});localStorage.setItem('sqRoom_'+code,JSON.stringify(room))}showRoom(room)}function showRoom(room){document.getElementById('mpForm').hidden=true;document.getElementById('roomInfo').hidden=false;document.getElementById('roomCodeDisplay').textContent=room.code;document.getElementById('playerList').innerHTML=room.players.map((p,i)=>`<div class="mp-player">${i===0?'👑':'🎮'} ${escapeHtml(p.name)}</div>`).join('')}function copyRoomCode(){const c=document.getElementById('roomCodeDisplay').textContent;if(navigator.clipboard)navigator.clipboard.writeText(c);alert('Room code: '+c)}function escapeHtml(s){return String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}window.addEventListener('DOMContentLoaded',()=>{const b=document.querySelector('.subject.hindi');if(b)b.addEventListener('click',()=>{setTimeout(()=>{if(document.getElementById('levelGrid')&&!document.getElementById('levelGrid').children.length&&typeof window.openLevels==='function')window.openLevels('hindi')},50)})});